<h1>Results page</h1>

<ul>
  <% data["Search"].forEach(function(movie) { %>
    <strong>
      <li><%= movie["Title"] %> 
    </strong> 
    - <%= movie["Year"] %> </li>
  <% }); %>
</ul>

<a href="/">Search Again</a>